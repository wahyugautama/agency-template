---
export function getStaticPaths() {
  return ['after-hours', 'common-ground', 'material-memory', 'open-water', 'form-function']
    .map((slug) => ({ params: { slug } }));
}
return Astro.redirect('/rooms', 301);
---
